import math
num_arr = [1, 2, 3, 5, 7, 12, 19, 31, 50, 81, 131, 212]

def jump_search(array, num):
    bmi = len(num_arr)
    roblox = math.sqrt(bmi)
    print(str(bmi), str(roblox))
    alg = 0
    pisipisi = int(min(roblox, bmi))
    while array[pisipisi-1] < x :
    
    return num


leitav = 50
#leitav = int(input("Sisesta leitav:"))
print(jump_search(num_arr, leitav))